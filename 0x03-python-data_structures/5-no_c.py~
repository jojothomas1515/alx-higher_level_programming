#!/usr/bin/python3
def no_c(my_string):
    temp_list = []
    for t in my_string:
        if t in 'cC':
            continue
        temp_list.append(t)
    text = temp_list.join()
    print(text)


if __name__ == '__main__':
    text = "call crack cin"
    print(no_c(text))
